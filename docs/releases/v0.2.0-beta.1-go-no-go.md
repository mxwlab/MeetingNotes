# v0.2.0-beta.1 Go / No-Go

评估日期：2026-08-13。结论：**NO-GO（可内部 RC，不可公开发布）**。

## 已通过

- 分支相对 `main`：0 个落后、45 个领先提交，无未知双向分叉。
- 发布包显式白名单 33 个文件，不包含 config、本地录音、纪要、日志或 API Key。
- 两次独立构建字节一致；zip 在 macOS `ditto` 解压后保留执行权限。
- Python 测试 30 个通过；全部 15 个 shell 测试通过。
- 真实短录音、29 秒完整状态链和 71 分 41 秒长录音均已在当前 Mac 通过。
- Python、ffmpeg 和 Qwen 模型入口在 2026-08-13 均返回有效 HTTP 响应。
- 原始音频只供本机 ffmpeg/Qwen 处理；对外发送的是转录文字。配置文件权限为 600，安装日志不写 Key。
- 已固定直接 Python/UI 依赖，并把 Qwen 模型固定到 revision `a8379a2e2f9e313c9292cdf1af4055ab56d50d55` 的项目本地快照。
- 当前 RC SHA-256：`c1d0e12ddd4216e02e5f63ae8450ebcb8d9314a27c8889749e5baef5e5564ed3`；两次独立构建一致。

## 阻塞公开发布

1. 必须在第二台无缓存 Apple Silicon Mac 完成冷下载、安装、重启自启、整段录音与 AirDrop 验收。
2. 必须补齐不含 Key/隐私内容的真实安装过程与菜单栏截图。
3. 需要完成模型与完整传递依赖的许可证归档策略；当前 NOTICE 已覆盖随安装下载的 GPL FFmpeg，直接 Python 依赖均为 MIT/BSD/Apache/PSF，但模型权重及传递依赖尚未形成随包清单。
4. 未签名、未公证。若面向陌生用户公开分发，应先确定是否接受“右键打开”的内测体验，或进入 Developer ID 签名与公证流程。

## 2026-08-13 同机隔离冷安装演练

- 从新构建的 RC zip 解压到中文空格路径，使用独立 HOME、安装目录、后台服务和菜单栏服务名，未覆盖当前正式实例。
- 独立 Python 3.10.20、全部转录依赖、FFmpeg 7.1、固定 revision 的 Qwen 模型（落盘约 2.2 GB）、后台监听、菜单栏 App、登录自启和 Folder Action 全部真实完成，约耗时 5 分钟。
- 安装后 App、Python、ffmpeg、模型与权限 600 的配置均核验通过；隔离菜单栏服务持续 running，正式菜单栏服务未受影响。
- 体验问题：第 3/8 Python 依赖、第 5/8 模型下载和第 7/8 菜单栏构建期间，详细进度被写入日志而没有持续显示在主窗口，普通用户可能误判为卡住。下一轮应增加明确的“正在工作/预计耗时/请勿关闭”反馈，模型下载最好提供体积或百分比。
- 本次证明了同一台 Mac 的新目录冷安装可行，但因系统级缓存和同一硬件环境，仍不能替代第二台真正干净 Mac 的发布门禁。

## 发布时才执行（本轮禁止）

- 合并到 `main`。
- 以最终提交重建 RC，填写最终 SHA。
- 创建 GitHub prerelease 和上传 zip、sha256、manifest。
- 从公开 Release URL 再做一次匿名下载与 SHA 校验。

## 当前测试数据（仅列出，不删除）

项目 `done/` 中仍有多段真实/验收录音，`output/` 中仍有对应转录、整理稿与纪要，其中包括“菜单栏状态链验收”“菜单栏长会议验收”“下午长计划安装后烟测”。这些数据不被 Git 跟踪，也不进入发布 zip；是否清理由用户另行决定。
